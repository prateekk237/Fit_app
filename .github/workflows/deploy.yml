name: Build & Deploy Fit

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-prod
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/fit-pwa

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      # Phase 16 enables the steps below. Left commented until the VPS is
      # prepared per Part 4 of the master plan (Dockerfile, secrets, etc.).
      #
      # - uses: docker/login-action@v3
      #   with:
      #     registry: ${{ env.REGISTRY }}
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      # - uses: docker/setup-buildx-action@v3
      # - uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: |
      #       ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
      #       ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max

      - name: Placeholder
        run: echo "Phase 0 — deploy workflow is parked until Phase 16."

  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Pre-deploy AssetShield check
  #       run: |
  #         STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://assetshield.co.in)
  #         if [ "$STATUS" != "200" ] && [ "$STATUS" != "301" ] && [ "$STATUS" != "302" ]; then
  #           echo "AssetShield unhealthy ($STATUS). Abort."
  #           exit 1
  #         fi
  #
  #     - name: Check VPS memory
  #       uses: appleboy/ssh-action@v1.2.0
  #       with:
  #         host: ${{ secrets.VPS_HOST }}
  #         username: ${{ secrets.VPS_USER }}
  #         key: ${{ secrets.VPS_SSH_KEY }}
  #         script: |
  #           AVAIL=$(free -m | awk '/^Mem:/ {print $7}')
  #           if [ "$AVAIL" -lt 700 ]; then
  #             echo "Insufficient RAM ($AVAIL MB). Need 700+."
  #             exit 1
  #           fi
  #
  #     - name: Deploy
  #       uses: appleboy/ssh-action@v1.2.0
  #       with:
  #         host: ${{ secrets.VPS_HOST }}
  #         username: ${{ secrets.VPS_USER }}
  #         key: ${{ secrets.VPS_SSH_KEY }}
  #         timeout: 300s
  #         script: |
  #           set -e
  #           cd /home/deploy/apps/fit
  #           echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
  #           docker compose -f docker-compose.prod.yml pull
  #           docker compose -f docker-compose.prod.yml run --rm fit-migrate
  #           docker compose -f docker-compose.prod.yml up -d --no-deps fit-app
  #           docker image prune -f
  #           for i in {1..20}; do
  #             if curl -sf http://127.0.0.1:3100/api/health > /dev/null; then
  #               echo "Fit healthy"; break
  #             fi
  #             sleep 3
  #           done
  #
  #     - name: Post-deploy verification
  #       run: |
  #         sleep 10
  #         ASSET=$(curl -s -o /dev/null -w "%{http_code}" https://assetshield.co.in)
  #         FIT=$(curl -s -o /dev/null -w "%{http_code}" https://fit.assetshield.co.in)
  #         echo "AssetShield: $ASSET | Fit: $FIT"
  #         if [ "$ASSET" != "200" ] && [ "$ASSET" != "301" ] && [ "$ASSET" != "302" ]; then
  #           echo "CRITICAL: AssetShield broke!"; exit 1
  #         fi
